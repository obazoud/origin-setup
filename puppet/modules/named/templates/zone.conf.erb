key <%= $zone %> {
  algorithm HMAC-MD5 ;;
  secret "<%= $zone %>" ;
} ;

zone "<%= $zone %>" IN {
	type master ;
	file "dynamic/<%= $zone %>.db" ;
        allow-update { key <%= $secret %> ; } ;
} ;
